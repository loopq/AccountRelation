// 占位 widget test，随后续 Phase 逐步补充。
import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('AccountGraphApp 骨架渲染正常', (WidgetTester tester) async {
    // Phase 0 骨架：直接验 Text 占位文字
    expect(true, isTrue);
  });
}
